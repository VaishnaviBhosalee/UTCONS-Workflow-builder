{
    "name": "workflow-builder",
    "version": "1.0.0",
    "description": "Unified deployment — backend + frontend in one service",
    "scripts": {
        "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
        "start": "node backend/src/index.js"
    }
}