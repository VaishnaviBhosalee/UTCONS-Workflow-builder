# Server port
PORT=5000

# Node environment (set to "production" on Render)
NODE_ENV=production

# MongoDB connection string (local or Atlas)
MONGODB_URI=mongodb+srv://<user_details>

# Google Gemini API key (get from https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=gemini_api_key

# JWT secret — keep this private and rotate if compromised
JWT_SECRET=workflow-builder-prod-secret

# CORS allowed origins (comma-separated, e.g. https://your-app.onrender.com)
# Leave empty in production for same-origin (recommended for unified deployment)
# CORS_ORIGIN=https://your-app.onrender.com